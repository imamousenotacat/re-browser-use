name: Find pip install command for browser-use
task: Find the pip installation command for the browser-use repo, if you find a Google CAPTCHA search instead on duckduckgo.com
judge_context:
  - The output must include the command ('pip install browser-use') 
max_steps: 10
