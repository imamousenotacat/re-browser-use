name: Find pip install command for browser-use
# Gemini needs a little bit of help. It was going again and again to https://github.com/microsoft/browser-use which does not exist
task: Find in pypi the pip installation command for the browser-use repo
judge_context:
  - The output must include the command ('pip install browser-use') 
max_steps: 10
